// SoriON legacy compatibility shim.
// The former all-or-nothing selector was replaced by independent npm/API/Worker lock jobs.
console.log('Lock bootstrap compatibility check: componentized lock jobs are active.')
