rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // SoriON currently uses Firebase Authentication only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
