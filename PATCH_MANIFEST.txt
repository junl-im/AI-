SoriON AI 0.9.3-beta.3 Engine Heartbeat 5.2 -> Engine Heartbeat 5.2.1 Focus Return Hotfix

Purpose: restore focus to the explicit project-menu trigger after modal dismissal and remove the React Hooks mutable-ref cleanup warning.

Key changes:
- capture previous focus and the explicit return-focus DOM node when the modal effect opens
- prefer a connected explicit return target over the transient previous active element
- avoid reading returnFocusRef.current inside effect cleanup
- preserve the existing Escape, focus trap and body-scroll-lock behavior
- add dependency-free regression contracts for focus-return priority and ref capture

Tracked files deleted relative to Engine Heartbeat 5.2: none
Cumulative permanent cleanup list:
public/sorion-icon.svg
