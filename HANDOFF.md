# HANDOFF v1.2.0

## 요약

v1.2.0은 사용자 용어와 런타임 응답성을 함께 정리한 안정화 패치입니다. 사용자에게 보이는 하단 내비게이션 명칭은 `Dock` 대신 **메뉴바**로 통일했습니다. 실제 브라우저에서 초기 DOM 로드 직후 메인 스레드가 계속 점유되는 현상을 재현했고, 여러 `MutationObserver`가 같은 값을 다시 쓰며 서로를 깨우던 피드백 루프를 제거했습니다.

## 페이지 응답 없음 원인과 수정

- 직접 원인은 추천 개수, 후보 목록, 메뉴 활성 상태를 감시하는 모듈 간의 반복 DOM 쓰기였습니다.
- 값이 바뀌지 않았는데도 `textContent`, 속성, 클래스와 `data-active-flow-tab`을 다시 쓰면서 새 Mutation이 발생했습니다.
- 새 Mutation이 다른 모듈의 `requestAnimationFrame` 갱신을 예약해 초기화 이후에도 반복이 멈추지 않았습니다.
- 모든 핵심 동기화는 **현재 값과 목표 값이 다를 때만** DOM을 변경하도록 수정했습니다.
- 상태 시그니처가 같으면 프레임 작업을 건너뛰고, 후보 목록 감시자는 자체 속성 변경을 다시 감지하지 않도록 범위를 좁혔습니다.

## 이번 패치의 핵심 파일

- `src/ui/flow-integrity.js`
- `src/ui/hyperflow-tabs.js`
- `src/ui/flow-polish.js`
- `src/ui/flow-doctor.js`
- `src/ui/workspace-comfort.js`
- `src/ui/candidate-pin-board.js`
- `src/ui/flow-director-final.js`
- `src/ui/flow-quality-gate.js`
- `src/boot/update-sentinel.js`
- `index.html`
- `sw.js`
- `qa/runtime_responsiveness_smoke.js`

## 사용자 디자인 선호

- PC와 모바일을 하나의 레이아웃으로 억지로 맞추지 않습니다.
- PC는 넓은 화면을 활용해 입력, 추천, 후보, 미리보기와 파형이 한눈에 들어오는 분할 작업실을 선호합니다.
- 모바일은 기능을 중복 노출하지 않고, 하단 메뉴바를 실행 수단으로 사용하며 본문은 진행 순서를 쉽게 보여줍니다.
- 개발자에게 익숙한 전문 용어보다 일반 사용자가 바로 이해하는 표현을 우선합니다. 사용자 노출 명칭은 `Dock`이 아니라 `메뉴바`입니다.
- 정보는 많아도 시선의 시작점과 다음 행동이 명확해야 합니다.
- 상단 메타 정보는 버전 왼쪽 끝, `Design by 곰같은여우` 오른쪽 끝처럼 정렬 기준이 분명해야 합니다.
- 제목 아래 설명은 화면 폭을 가득 채우지 않고 읽기 좋은 최대 폭과 좌우 여백을 둡니다.
- 영화·카메라 분위기의 글래스 표현은 유지하되 실제 작업 공간을 침범하지 않게 절제합니다.

## 유지해야 할 동작

- 1180px 이상에서는 `data-desktop-layout="prime"`의 3열 작업실이 적용되어야 합니다.
- PC에서 불러오기, 추천, 미리보기, 후보와 파형 패널은 활성 메뉴와 무관하게 동시에 보여야 합니다.
- PC 메뉴바 버튼은 기존 상태·비활성 규칙을 유지하며 해당 카드로 이동해야 합니다.
- 720px 이하이고 파일이 없는 `file` 단계에서는 본문 불러오기 패널을 표시하지 않습니다.
- 모바일 파일 열기는 하단 메뉴바의 `label[for="fileInput"]`가 담당합니다.
- 모바일 진행 순서는 불러오기 → 자동 분석 → 편집 → 내보내기 2x2 카드로 유지합니다.
- 파일이 로드되면 `body.has-media`에 의해 모바일 진행 안내가 접혀야 합니다.
- 사용자에게 보이는 설명, 접근성 라벨과 도움말에서 `Dock` 용어를 다시 사용하지 않습니다.
- `MutationObserver` 콜백 안에서 같은 텍스트·속성·클래스를 무조건 다시 쓰지 않습니다.
- 관찰 대상에 자신이 변경하는 속성을 포함해야 한다면 반드시 값 비교 또는 상태 시그니처 가드를 둡니다.
- 500~700ms 상시 폴링을 다시 추가하지 않습니다.
- 버전, 빌드 키와 서비스워커 캐시 키는 항상 함께 올립니다.

## 검수 순서

1. `npm run check`
2. 페이지를 연 뒤 메뉴 클릭, 스크롤과 입력이 즉시 반응하는지 확인
3. 개발자 도구 Performance에서 초기화 뒤 지속적인 RAF·Mutation 폭주가 없는지 확인
4. 1440px 이상 PC에서 첫 화면에 3열 분할 작업실이 보이는지 확인
5. PC에서 불러오기·추천·미리보기·후보·파형이 동시에 보이는지 확인
6. 메뉴바의 추천·후보·미리보기·파형 버튼이 해당 카드로 이동하는지 확인
7. 390px 모바일에서 버전과 디자인 서명이 같은 줄 양 끝에 있는지 확인
8. 모바일 첫 화면에 파일·프로젝트 불러오기 버튼이 중복 표시되지 않는지 확인
9. 모바일에서 4단계 진행 카드와 하단 메뉴바의 파일 열기만 보이는지 확인
10. 실제 미디어로 분석 → 추천 → 후보 선택 → 미리보기 → 저장 E2E 확인

## 알려진 제한

- PC Prime 작업실의 칼럼 폭은 현재 고정 비율이며 사용자가 직접 드래그해 조절할 수 없습니다.
- 1180px 미만 태블릿은 기존 단계형 작업 흐름을 유지합니다.
- 원본 미디어 파일은 브라우저 보안 정책상 프로젝트 JSON에 포함되지 않습니다.
- 모바일 Safari 및 일부 인앱 브라우저에서는 MediaRecorder 출력 형식이 제한될 수 있습니다.
- 실제 대용량 MP4를 사용한 장시간 렌더 성능은 자동 QA가 아니라 브라우저 실기기 검증이 필요합니다.

## 다음 예상 진행

1. 실제 MP3·MP4 기반 E2E와 브라우저별 저장 형식 검증
2. PC 칼럼 드래그 리사이즈와 미리보기·파형 집중 모드
3. 후보 카드 비교·고정과 미리보기 연결 UX 정리
4. CSS 38개와 스크립트 54개의 번들·단계별 지연 로딩
5. 모바일 메뉴바 단계 축약과 엄지손가락 동선 검증

## 기존 기반 유지

Update Sentinel, Engine Boost Profile, 세션 연속성, 렌더 큐와 저장 준비도 모듈은 유지합니다. 내부 파일명과 DOM ID의 `dock` 표현은 호환성을 위해 그대로 두되 사용자 노출 문구에는 사용하지 않습니다.
