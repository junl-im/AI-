# AI 쇼츠 제작 스튜디오 v0.2.0

## Current patch: v0.2.0 편집·자막·프로젝트 업그레이드

- Runtime asset cache key: `0.2.0-editor-caption-project`
- Service worker cache: `ai-shorts-studio-shell-v0.2.0-editor-caption-project`
- Package version: `0.2.0`
- Manual QA doc: `qa/QA_REPORT.md`

## 목표

`AI 쇼츠 제작 스튜디오`는 사용자가 가진 풀 노래 또는 MP4/MOV/WebM 영상을 브라우저 안에서 분석하고, 쇼츠용 하이라이트 구간을 추천한 뒤 9:16 세로 영상으로 미리보기/내보내기하는 무료 웹 프로그램입니다.

서버 업로드 없이 로컬 브라우저에서 처리하는 것을 기본 원칙으로 합니다.

## v0.2.0 기능

- 오디오/비디오 파일 불러오기
- Web Audio 기반 로컬 오디오 분석
- MP4 등 비디오의 오디오 트랙 분석 시도
- 비디오 장면 움직임 샘플링 보조 분석
- 15초, 30초, 45초, 60초, 90초, 180초 후보 추천
- 추천 점수와 추천 이유 카드 제공
- 파형 위 추천 구간 표시
- 9:16 세로 캔버스 미리보기
- 중앙 크롭, 상단 크롭, 하단 크롭, 블러 배경 맞춤 프레임
- 추천 구간 시작/끝 시간 수동 조절
- SRT/VTT 자막 업로드 및 붙여넣기
- 빠른 텍스트 자막 자동 분할 fallback
- 자막 스타일 3종: 굵은 쇼츠 자막, 깔끔한 하단 자막, 검정 박스 자막
- 자막 싱크 보정
- 썸네일 PNG 저장
- 프로젝트 JSON 저장/불러오기
- 쇼츠 제목/해시태그 초안 생성
- MediaRecorder 기반 로컬 내보내기
- 다운로드/공유 진단 복사
- PWA 기본 캐시
- QA smoke test 패키지

## 무료 기능 기준의 현실적 범위

이 버전은 유료 생성형 AI API를 사용하지 않습니다. 추천은 다음 로컬 분석값으로 계산합니다.

- RMS 에너지
- 피크 밀도
- 트랜지언트 변화량
- 무음/저에너지 비율
- 에너지 상승 구간
- 비디오 프레임 차이 기반 움직임 점수

따라서 “AI가 새 영상을 생성”하는 방식이 아니라, **사용자 파일에서 쇼츠로 쓸 만한 구간을 자동 큐레이션하고 세로 영상으로 편집하는 방식**입니다.

## 사용법

1. `npm run serve` 또는 정적 서버로 실행합니다.
2. 브라우저에서 `http://localhost:8080`을 엽니다.
3. 오디오 또는 비디오 파일을 불러옵니다.
4. `분석하고 추천받기`를 누릅니다.
5. 추천 카드에서 구간을 선택합니다.
6. 필요한 경우 `시작 시간`과 `끝 시간`을 수정한 뒤 `선택 구간 적용`을 누릅니다.
7. SRT/VTT 자막을 열거나 자막 텍스트를 붙여넣고 `자막 적용`을 누릅니다.
8. `선택 구간 미리보기`로 확인합니다.
9. `썸네일 PNG 저장` 또는 `세로 쇼츠 내보내기`로 저장합니다.
10. 작업 상태를 이어가려면 `프로젝트 저장`으로 JSON을 보관합니다.

## 프로젝트 JSON 주의사항

프로젝트 파일에는 원본 미디어가 포함되지 않습니다. 저장되는 항목은 다음과 같습니다.

- 추천 구간
- 직접 수정한 시작/끝 시간
- 자막 큐
- 자막 스타일/싱크 설정
- 제목/해시태그
- 앱 설정

다시 열 때는 같은 원본 파일을 먼저 불러온 뒤 프로젝트 JSON을 적용하는 방식이 가장 안전합니다.

## QA

```bash
npm run check
```

v0.2.0 기준 자동 QA 결과:

```text
AI Shorts Studio QA summary
  Passed: 25/25
  Failed: 0/25
```

## 패키징

```bash
npm run package:clean
```

결과물은 `dist/ai-shorts-studio-v0.2.0-release.zip`에 생성됩니다.

## 브라우저 주의사항

- MP4로 저장 가능 여부는 브라우저의 `MediaRecorder` 지원 범위에 따라 달라집니다.
- Chrome 계열은 WebM 내보내기가 안정적입니다.
- Safari/iOS는 `captureStream`, `MediaRecorder`, 비디오 코덱 지원이 제한될 수 있습니다.
- 이 앱은 서버에 파일을 업로드하지 않습니다.

## 저작권 안내

업로드한 음원과 영상의 사용 권리는 사용자가 확인해야 합니다. 이 프로그램은 편집과 추천을 돕는 도구이며, 플랫폼 게시 권리나 Content ID 문제를 자동으로 해결하지 않습니다.
