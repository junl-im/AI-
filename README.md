# AI 쇼츠 제작 스튜디오 v1.6.1

브라우저에서 미디어 분석, 쇼츠 후보 추천, 자막 편집, 세로 영상 렌더링을 수행하는 로컬 우선 제작 도구입니다. v1.6.1은 일반 사용자에게 노출되던 저장소·오프라인 내부 진단을 간단한 상태 요약과 별도 고급 진단 창으로 분리합니다.

## 핵심 기능

기존 **모듈형 엔진** 계약 위에서 다음 기능이 동작합니다.

- 오디오·영상 움직임 분석과 쇼츠 후보 추천
- 자동 컷, 후보 고정, 자막·SRT 편집, 9:16 렌더 큐
- 옵션 signature 기반 분석 캐시와 namespace 선택 정리
- 세션 순환 백업과 중요 백업 내보내기·가져오기
- 서비스워커 앱 셸 SHA-256 무결성 검사와 복구
- Ollama·llama.cpp 구조화 카피 생성
- whisper.cpp·OpenAI 호환 로컬 전사 및 SRT 생성
- Local AI 모델 digest 고정, 직렬 작업 큐, 취소·시간 제한·진행률

## v1.6.1 저장소 화면

일반 화면에는 다음 항목만 표시합니다.

- 저장소와 오프라인 기능의 정상·주의·문제 상태
- 현재 저장 공간 사용량
- 실제 조치가 필요할 때만 나타나는 `저장 공간 정리` 버튼
- 사용자가 명시적으로 여는 `고급 진단` 버튼

`namespace`, `signature`, 셸 무결성 표본, 분석 캐시 선택 삭제, 감사 이력 같은 기술 기능은 고급 진단 창 안에만 있습니다. 고급 창을 열기 전에는 분석 캐시 상세 목록을 조회하지 않습니다. 캐시 전체 삭제 등 위험 작업은 프로젝트 원본과 편집 데이터가 삭제되지 않는다는 안내와 별도 확인을 거칩니다.

## 로컬 AI 원칙

1. 페이지를 열기만 해서는 AI 서버에 요청하지 않습니다.
2. 연결 확인·카피 생성·전사는 사용자의 명시적 동작 뒤에만 실행됩니다.
3. 기본 설정에서는 loopback 주소만 허용합니다.
4. 외부 CDN, 원격 모델, API 키는 배포본에 포함하지 않습니다.
5. Local AI가 없거나 실패해도 기존 분석·편집·렌더 기능은 계속 동작합니다.
6. Local AI 모듈은 해당 영역을 처음 사용할 때만 단계 적재합니다.

## 실행

```bash
npm run serve
```

브라우저에서 `http://127.0.0.1:8080`을 엽니다. Local AI를 사용하려면 별도로 설치한 서버를 loopback 주소에 실행하고 이 앱 origin을 허용하도록 CORS를 설정해야 합니다.

## 검수

```bash
npm test
```

v1.6.1은 자동 검사 219개, 일반 화면 4개 viewport 감사, 저장소 진단 전용 데스크톱·모바일 감사, CSS ownership, 서비스워커 lifecycle, GPU/media capability, 프로세스 메모리 보조 감사를 수행합니다.

## 패키징

```bash
npm run package:full
PATCH_BASE_REF=a8c95f7 PATCH_FROM_VERSION=1.6.0 npm run package:patch
```

전체 ZIP은 실행·문서·QA 파일을 포함하고 `.git`, `node_modules`, 중첩 `dist`를 제외합니다. 패치 ZIP은 v1.6.0 기준 변경·신규 파일만 포함합니다.

## 알려진 제한

- AI 모델과 로컬 서버 실행 파일은 배포본에 포함되지 않습니다.
- 실기기 Safari·Samsung Internet, 물리 GPU·NPU는 별도 검증이 필요합니다.
- 브라우저 보안 정책 때문에 Local AI 서버의 CORS 설정이 필요할 수 있습니다.
- 프로세스 메모리 수치는 headless Chromium 환경의 보조 지표입니다.
