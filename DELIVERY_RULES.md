# SoriON AI 절대 전달 규칙

상태: **필수**  
적용 범위: SoriON AI의 모든 개발 결과 전달, 릴리스, 패치, 인수인계  
변경 조건: 사용자의 명시적 승인 없이는 완화하거나 삭제하지 않는다.

## 1. 결과

모든 최종 응답의 첫 번째 항목은 반드시 `1. 결과`여야 한다.

포함할 내용:

- 이번 작업에서 완료한 변경 사항
- 사용자에게 보이는 영향
- 실행한 검사와 결과
- 실행하지 못한 검사와 이유
- 알려진 제한 또는 주의사항

완료하지 못한 항목을 완료한 것처럼 표현하지 않는다.

## 2. 전체 통파일 ZIP과 덮어쓰기용 패치 ZIP

모든 코드 업데이트 결과에는 다음 두 파일을 함께 제공한다.

### 전체 통파일 ZIP

- 현재 버전의 전체 프로젝트를 포함한다.
- 새 폴더에 압축 해제해 독립적으로 사용할 수 있어야 한다.
- 최상위 폴더명에 버전을 포함한다.
- `.git`, `node_modules`, `dist`, 가상환경, 캐시, 비밀키를 포함하지 않는다.

권장 이름:

```text
SoriON-AI-{version}-full.zip
```

### 덮어쓰기용 패치 ZIP

- 직전 기준 버전에서 현재 버전으로 변경된 파일과 새 파일만 포함한다.
- ZIP 내부 경로는 저장소 루트 기준이어야 한다.
- 기존 저장소 루트에 바로 압축 해제해 덮어쓸 수 있어야 한다.
- `.git` 폴더를 포함하거나 변경하지 않는다.
- `docs/patches/{version}/PATCH_README.md`와 `PATCH_MANIFEST.txt`를 포함한다.
- 파일 삭제가 필요하면 `DELETE_LIST.txt`에 명시하고 결과 설명에서도 알린다.

권장 이름:

```text
SoriON-AI-{from-version}-to-{version}-patch.zip
```

두 ZIP의 SHA-256 체크섬도 함께 생성한다.

## 3. 다음 예상 업데이트 내역

모든 최종 응답의 마지막 항목은 반드시 `3. 다음 예상 업데이트 내역`이어야 한다.

포함할 내용:

- 다음 목표 버전
- 구현할 핵심 기능
- 예상 변경 파일 또는 영역
- 선행 조건과 위험 요소
- 이번 작업에서 다음 작업으로 넘기는 결정 사항

다음 계획은 `docs/NEXT_UPDATE.md`에도 동일한 방향으로 기록한다.

## 인수인계 기록 의무

모든 업데이트에서 `docs/HANDOVER.md`에 새 항목을 추가한다. 기존 기록을 삭제하거나 덮어쓰지 않는다.

각 기록에는 반드시 다음 항목이 있어야 한다.

1. 작업 일시(KST)
2. 대상 버전과 기준 버전
3. 변경 내용
4. 변경 이유
5. 영향 범위
6. 변경·추가된 주요 파일
7. 검증 결과
8. 알려진 제한과 주의사항
9. 생성한 전체 ZIP과 패치 ZIP 이름
10. 다음 예상 업데이트

동시에 `docs/CHANGELOG.md`, `docs/NEXT_UPDATE.md`, 필요 시 관련 설계 문서도 갱신한다.

## 배포 차단 조건

다음 중 하나라도 누락되면 결과 전달을 완료로 처리하지 않는다.

- `1. 결과` 설명
- 전체 통파일 ZIP
- 덮어쓰기용 패치 ZIP
- 체크섬
- `docs/HANDOVER.md` 신규 기록
- `docs/CHANGELOG.md` 현재 버전 기록
- `docs/NEXT_UPDATE.md` 다음 계획
- `3. 다음 예상 업데이트 내역` 설명

## 패치 적용 원칙

1. 패치의 기준 버전이 현재 로컬 버전과 일치하는지 확인한다.
2. 작업 중인 변경사항을 먼저 커밋하거나 백업한다.
3. `.git`은 삭제하거나 덮어쓰지 않는다.
4. 패치 ZIP을 저장소 루트에 압축 해제한다.
5. GitHub Desktop의 Changes에서 변경 파일을 확인한다.
6. 검사 후 커밋하고 Push한다.

이 문서는 SoriON AI 프로젝트의 영구 전달 기준이다.
