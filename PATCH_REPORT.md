# v1.6.13 Patch Report

## 문제 1: 모델 팩 설치 중 기존 팩 선삭제

최대 설치 수에 도달한 상태에서 신규 팩을 설치하면 오래된 비활성 팩을 먼저 삭제했습니다. 이후 Cache Storage 쓰기나 quota 오류가 발생하면 기존 팩은 사라지고 신규 팩 일부만 남을 수 있었습니다.

## 해결 1

- 기존 팩 삭제를 신규 팩 완전 저장 뒤로 이동
- 쓰기 성공 파일 목록 추적
- 중간 실패 시 부분 저장 신규 파일 자동 정리
- 메타데이터 저장 여부 재확인
- 실패 시 기존 inventory와 활성 선택 유지

## 문제 2: 동일 모델 backend 전환 실패의 잘못된 롤백

현재 모델 A를 GPU에서 CPU로 바꾸는 중 실패하면, 현재 A/GPU가 아니라 과거 모델 B 롤백 슬롯으로 이동할 수 있었습니다.

## 해결 2

- 현재 실행 팩과 실제 backend를 별도 임시 복구 후보로 캡처
- 같은 모델 backend 전환 실패 시 마지막 정상 backend로 복구
- 이전 모델 롤백 슬롯은 그대로 유지

## 문제 3: 반복 내보내기 Object URL 누적

다운로드 URL의 45초 지연 해제만 사용해 짧은 간격으로 내보내기를 반복하면 export URL이 매 회차 누적됐고, 종료 이벤트가 `beforeunload` 하나에만 의존해 감사 종료 시 URL이 남았습니다.

## 해결 3

- 새 내보내기 직전에 이전 export URL을 해제해 활성 다운로드 URL을 최대 1개로 제한
- 예약 타이머와 수동 해제를 동일한 소유권 맵에서 관리
- `beforeunload`와 `pagehide`에서 잔여 다운로드 URL 전부 해제
- 실미디어 반복 렌더 감사에서 회차별 export URL 상한과 dispose 후 0개를 검증

## 개발·검수 개선

- 검사별 180초 기본 타임아웃
- 인덱스 범위, 문자열 검색, 결정적 샤드 실행
- fail-fast와 JSON 결과 보고서
- 빈 필터를 성공으로 오인하지 않도록 명시 실패

## 개인정보·보안

영상·얼굴·모델 파일은 외부 서버로 전송되지 않습니다. 모델 팩 벤치마크는 로컬 합성 프레임을 사용하며, 성능 기록에는 팩 식별자·backend·시간 통계만 저장됩니다.
