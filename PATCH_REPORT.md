# PATCH REPORT v1.6.3

## 목표

모든 데스크톱 작업 패널이 동시에 공간을 차지해 현재 작업의 우선순위가 흐려지고 빈 행이 남는 문제를 해결합니다.

## 적용 내용

- 현재 단계 1개를 주 패널로 확장
- 바로 이어지는 단계만 86px 지원 카드로 축소
- 이후 단계와 선택형 유틸리티를 현재 흐름에서 제외
- 지원 카드 클릭 시 해당 단계 즉시 승격
- 전체 보기로 모든 패널 복구
- 미리보기·파형 전용 보기와 모바일 흐름은 기존 동작 유지
- 실제 하단 메뉴 높이를 측정해 콘텐츠 안전 여백 자동 적용
- 새 모듈을 shell staged phase에 포함하되 직접 실행 스크립트 49개 유지
- CSS 강제 우선순위 총량을 늘리지 않고 동적 상태 소유권으로 처리

## 검증 결과

- 자동 검사 224/224
- 4개 viewport 오류·가로 overflow 0
- 단계 집중 전용 데스크톱·모바일 감사 통과
- CSS 충돌·동일값 중복·shadow 0, `!important` 593
- 서비스워커 앱 셸 자산 125개 SHA-256 검증
- GPU/media, 프로세스 메모리, 장시간 영상 계약 통과
