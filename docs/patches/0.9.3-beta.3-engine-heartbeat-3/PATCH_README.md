# SoriON AI 0.9.3-beta.3 Engine Heartbeat 3 Patch

Base: `0.9.3-beta.3 Engine Heartbeat 2`

This patch relaxes the project line-count policy, adds the Engine Doctor connection and setup dashboard, and automatically connects the local `voice-presets` folder when the bundled engine launchers start the API.

Apply by overwriting the base folder, reviewing changes in GitHub Desktop, committing and pushing. No tracked file is deleted relative to Engine Heartbeat 2. `APPLY_PATCH.cmd` remains available for cumulative stale SVG cleanup and local preflight.
