# PROJECT NOTES v1.2.1

## 설계 결정

- 반응형은 단순 축소가 아니라 기기별 작업 방식으로 나눕니다.
- PC 첫 작업 프레임은 `불러오기/추천 | 미리보기 | 후보/파형`의 3열 구조입니다.
- 컷, 편집, 저장과 프로젝트는 같은 그리드의 다음 행에 배치합니다.
- 모바일 첫 화면에는 실행 버튼을 중복 배치하지 않습니다. 파일 선택은 하단 메뉴바가 담당합니다.
- 사용자에게 보이는 하단 내비게이션 명칭은 `메뉴바`로 통일합니다. 내부 클래스·ID·파일명의 `dock`은 호환성을 위해 유지합니다.
- PC 상단은 제품 정체성과 첫 행동을 동시에 보여주는 `브랜드 카피 | 스튜디오 상태 데크` 2열 구조를 사용합니다.
- PC의 상단 시작 버튼은 새 입력 상태를 만들지 않고 기존 `fileInput`에 연결합니다.
- 860px 이하에서는 상단 시작 데크를 제거해 모바일·좁은 태블릿의 메뉴바 시작 흐름과 중복되지 않게 합니다.
- 상단 장식은 필름·카메라 분위기를 내는 동시에 로컬 처리, 준비 상태와 출력 플랫폼 정보를 전달해야 합니다.
- 상태 동기화는 사용자 이벤트, 커스텀 흐름 이벤트와 필요한 범위의 MutationObserver를 사용합니다.
- DOM 동기화는 멱등적이어야 합니다. 현재 값과 목표 값이 같으면 텍스트, 속성, 클래스와 dataset을 다시 쓰지 않습니다.
- Observer가 감시하는 노드를 같은 콜백에서 수정할 때는 상태 시그니처 또는 값 비교 가드를 필수로 둡니다.
- 반복 폴링보다 이벤트 기반 한 프레임 배치를 우선합니다.

## 상단 디자인 원칙

1. 첫 2초 안에 제품명, 무엇을 하는 도구인지, 어디서 시작하는지가 보여야 합니다.
2. “멋짐”은 장식 개수보다 강한 타이포그래피, 정렬, 명암과 정보 계층으로 만듭니다.
3. 상태 데크는 장식용 대시보드가 아니라 `READY`, 로컬 처리와 작업 순서를 알려야 합니다.
4. PC는 넓은 공간을 활용하지만 상단이 작업실 전체를 가리지 않도록 다음 작업 영역의 시작이 첫 화면에 남아야 합니다.
5. 모바일은 동일한 PC 데크를 축소하지 않고 제거·압축해 기능 중복을 막습니다.
6. 애니메이션은 느리고 절제된 한두 개의 흐름만 사용하며, 저사양·모션 감소 환경에서는 중단합니다.
7. 접근 가능한 실제 `label[for="fileInput"]`을 사용하고 키보드 포커스를 명확히 표시합니다.

## 페이지 멈춤 회귀 방지 규칙

1. `textContent`, `innerHTML`, `setAttribute`, `classList`와 `dataset` 쓰기 전에 현재 값을 비교합니다.
2. MutationObserver의 `attributes: true`는 필요한 `attributeFilter`가 있을 때만 사용합니다.
3. 렌더 함수는 동일 상태로 연속 호출되어도 새 Mutation을 만들지 않아야 합니다.
4. 초기화 뒤 RAF와 Mutation 콜백 수가 계속 증가하는지 브라우저 프로브로 확인합니다.
5. 새 UI 모듈은 기존 동기화 모듈과 동일한 DOM 소유권을 중복해서 갖지 않습니다.

## 사용자 스타일 키워드

`와, 이거 멋지다`, `시네마틱 컨트롤 데크`, `강한 타이포그래피`, `PC 분할 작업실`, `모바일 단순 진행`, `기능 중복 제거`, `일반 사용자 언어`, `메뉴바`, `양 끝 정렬`, `넉넉한 텍스트 여백`, `영화적이지만 실용적인 글래스 UI`, `한눈에 다음 행동 파악`

## 다음 우선순위

1. 초기 CSS·스크립트 로딩 다이어트와 단계별 지연 로딩
2. 실제 MP3·MP4 기반 분석 → 추천 → 미리보기 → 저장 E2E
3. PC 분할 칼럼 드래그 조절과 미리보기·파형 집중 모드
4. 후보 카드 비교·고정과 미리보기 연결 UX 정리
5. 모바일 메뉴바 단계 축약과 엄지손가락 동선 검증
