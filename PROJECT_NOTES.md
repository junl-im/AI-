# PROJECT NOTES - v0.9.7

## 안정화 메모

- `body[data-ui="hyperflow-tabs"]`가 탭식 UI의 핵심 스위치입니다. 이 값이 바뀌면 모든 패널이 아래로 노출됩니다.
- 추천 생성과 후보 선택을 같은 탭에 묶지 않습니다. 추천은 생성, 후보는 선택입니다.
- 사용자의 위치를 방해하지 않기 위해 탭 전환은 스크롤을 강제로 움직이지 않습니다.
- CTA 버튼은 컴팩트하게 유지합니다. 큰 배너형 버튼은 파일 열기/추천 생성 같은 핵심 액션에만 제한합니다.
